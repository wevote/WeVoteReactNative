import { StyleSheet } from 'react-native';


const bookmark_action_styles = StyleSheet.create({
  bookmark_action: {
    position: 'absolute',
    top: 0,
    right: 0,
},
});

export default bookmark_action_styles;